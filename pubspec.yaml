name: journal
description: A custom journal engine for tracking anything.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  uuid: ^4.3.3
  iconoir_flutter: ^7.11.0
  share_plus: ^7.2.2
  url_launcher: ^6.2.4
  package_info_plus: ^5.0.1
  google_fonts: ^6.1.0
  intl: ^0.19.0
  fl_chart: ^0.66.0
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
